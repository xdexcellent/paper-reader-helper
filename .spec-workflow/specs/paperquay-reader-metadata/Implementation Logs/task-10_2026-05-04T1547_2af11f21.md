# Implementation Log: Task 10

**Summary:** Added the dedicated /paper/:paperId/reader route orchestration with PaperDetail loading, unread-to-reading auto-marking, PDF blob loading/revocation, parse-needed refresh, notes save, reading state save, and library-detail Open reader navigation. Added route-level coverage and jsdom URL cleanup support for the reader flow while preserving /paper/:paperId detail routing.

**Timestamp:** 2026-05-04T15:47:00.724Z
**Log ID:** 2af11f21-61f5-441c-9003-b69d57d5f17f

---

## Statistics

- **Lines Added:** +318
- **Lines Removed:** -1
- **Files Changed:** 5
- **Net Change:** 317

## Files Modified
- frontend/src/App.tsx
- frontend/src/App.test.tsx
- frontend/src/components/library/LibraryPage.tsx
- frontend/src/components/reader/ReaderToolbar.tsx

## Files Created
- frontend/src/components/reader/ReaderPage.tsx

---

## Artifacts

### Components

#### ReaderPage
- **Type:** React
- **Purpose:** Route-level reader orchestrator for /paper/:paperId/reader; loads paper detail, supplies ReaderShell state/callbacks, manages PDF blob URLs, parse refresh, notes save, reading state save, and back navigation.
- **Location:** frontend/src/components/reader/ReaderPage.tsx
- **Props:** { refreshLibrary: () => Promise<void> }
- **Exports:** ReaderPage

#### ReaderToolbar
- **Type:** React
- **Purpose:** Updated reader mode controls so the PDF mode button has a stable accessible name for route-level interaction tests.
- **Location:** frontend/src/components/reader/ReaderToolbar.tsx
- **Props:** { paper, mode, isUpdatingReadingState, onBack, onModeChange, onReadingStateChange }
- **Exports:** ReaderToolbar

### Functions

#### revokeBlobUrl
- **Purpose:** Safely revokes object URLs only when URL.revokeObjectURL exists, keeping browser cleanup while avoiding jsdom crashes.
- **Location:** frontend/src/components/reader/ReaderPage.tsx:22
- **Signature:** (url: string) => void
- **Exported:** No

#### loadPaper
- **Purpose:** Fetches PaperDetail for the route paper ID with request ordering so stale detail responses do not replace current state.
- **Location:** frontend/src/components/reader/ReaderPage.tsx:60
- **Signature:** (loading?: boolean) => Promise<void>
- **Exported:** No

#### loadPdf
- **Purpose:** Loads authenticated PDF blob URL via getPdfBlobUrl, revokes stale blob URLs, and records retryable errors for PdfReaderPane.
- **Location:** frontend/src/components/reader/ReaderPage.tsx:80
- **Signature:** () => Promise<void>
- **Exported:** No

#### handleModeChange
- **Purpose:** Switches reader modes and revokes current PDF blob URLs when leaving PDF mode.
- **Location:** frontend/src/components/reader/ReaderPage.tsx:155
- **Signature:** (nextMode: ReaderMode) => void
- **Exported:** No

#### handleParse
- **Purpose:** Runs parsePaper, waits for task completion when a task_id is returned, refreshes PaperDetail, and refreshes the library list.
- **Location:** frontend/src/components/reader/ReaderPage.tsx:164
- **Signature:** () => Promise<void>
- **Exported:** No

#### handleNotesSave
- **Purpose:** Persists reader notes through updatePaperNotes, merges returned paper fields into current PaperDetail, and refreshes the library list while allowing failures to propagate to ReaderNotesPanel.
- **Location:** frontend/src/components/reader/ReaderPage.tsx:179
- **Signature:** (notes: string) => Promise<void>
- **Exported:** No

#### handleReadingStateChange
- **Purpose:** Persists explicit reader reading status/progress changes through updatePaperReadingState, merges returned fields, and refreshes the library list.
- **Location:** frontend/src/components/reader/ReaderPage.tsx:191
- **Signature:** (payload: { reading_status: ReadingStatus; reading_progress: number }) => Promise<void>
- **Exported:** No

### Integrations

#### Integration
- **Description:** App registers the focused reader route separately from the existing detail route.
- **Frontend Component:** App
- **Backend Endpoint:** N/A frontend route /paper/:paperId/reader
- **Data Flow:** React Router /paper/:paperId/reader -> ReaderPage -> fetchPaperDetail -> ReaderShell; /paper/:paperId continues to render LibraryPage detail.

#### Integration
- **Description:** Library detail Open reader action navigates to the focused reader route without changing existing selection/detail behavior.
- **Frontend Component:** LibraryPage
- **Backend Endpoint:** N/A frontend navigation
- **Data Flow:** PaperMetadataPanel Open reader -> LibraryPage onOpenReader -> navigate(/paper/:id/reader) -> App ReaderPage route.

#### Integration
- **Description:** ReaderPage loads authenticated PDFs through the existing API wrapper and revokes blob URLs on mode switch, paper switch, stale response, and unmount.
- **Frontend Component:** ReaderPage + PdfReaderPane
- **Backend Endpoint:** GET /papers/{paper_id}/pdf via getPdfBlobUrl
- **Data Flow:** User selects PDF mode -> getPdfBlobUrl(paper.id) -> blob URL stored -> PdfReaderPane iframe src -> Markdown mode/unmount revokes current blob URL.

#### Integration
- **Description:** ReaderPage automatically marks unread papers as reading without overwriting read or skipped states.
- **Frontend Component:** ReaderPage + ReaderToolbar
- **Backend Endpoint:** PATCH /papers/{paper_id} via updatePaperReadingState
- **Data Flow:** fetchPaperDetail returns unread -> updatePaperReadingState({ reading_status: 'reading', reading_progress }) -> local PaperDetail status merge -> refreshLibrary; read/skipped states skip the call.

#### Integration
- **Description:** ReaderPage handles missing Markdown by triggering parse and refreshing detail after task completion.
- **Frontend Component:** ReaderPage + MarkdownReaderPane
- **Backend Endpoint:** POST /papers/{paper_id}/parse via parsePaper and GET /tasks/{task_id} via waitForTaskCompletion
- **Data Flow:** MarkdownReaderPane Parse paper -> ReaderPage parsePaper -> optional waitForTaskCompletion -> fetchPaperDetail -> MarkdownReaderPane renders refreshed Markdown.

#### Integration
- **Description:** Route-level tests cover reader navigation and failure states with mocked API wrappers.
- **Frontend Component:** App.test.tsx
- **Backend Endpoint:** Mocked api.ts wrappers: fetchPaperDetail, getPdfBlobUrl, updatePaperReadingState, parsePaper, waitForTaskCompletion
- **Data Flow:** MemoryRouter initial entries and UI clicks -> App route -> ReaderPage effects/callbacks -> mocked api.ts assertions and visible reader states.

