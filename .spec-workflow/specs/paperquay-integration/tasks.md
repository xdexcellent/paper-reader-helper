# Tasks Document

## File Map

- `frontend/src/components/library/libraryTypes.ts`: shared frontend-only types for the Phase 1 library shell.
- `frontend/src/components/library/libraryFilters.ts`: pure search, filter, tag, category, and duplicate-title helpers.
- `frontend/src/components/library/libraryFilters.test.ts`: focused unit tests for library helper behavior.
- `frontend/src/lib/api.ts`: existing frontend API wrapper, extended only to send an optional upload title.
- `backend/tests/test_upload_paper.py`: backend regression coverage for the existing `/papers/upload` title override field.
- `frontend/src/components/library/ImportConfirmDialog.tsx`: PDF selection and metadata confirmation before upload.
- `frontend/src/components/library/LibrarySidebar.tsx`: category scope navigation and library counters.
- `frontend/src/components/library/LibraryToolbar.tsx`: import, refresh, create-category, and parse-failure actions.
- `frontend/src/components/library/PaperLibraryList.tsx`: dense paper list with search, status, category, and tag filtering.
- `frontend/src/components/library/PaperMetadataPanel.tsx`: selected-paper metadata, category, tags, status, and reader entry.
- `frontend/src/components/library/PaperOverviewPanel.tsx`: PaperQuay-style overview layout mapped from existing summary fields.
- `frontend/src/components/library/LibraryPage.tsx`: orchestration layer for the new paper management route.
- `frontend/src/components/library/CategoryCreateForm.tsx`: controlled create-category form used by the route orchestrator.
- `frontend/src/components/library/LibraryDetailStack.tsx`: selected-paper action, metadata, feedback, and overview stack.
- `frontend/src/components/library/LibraryImportModal.tsx`: modal shell for confirmed PDF imports.
- `frontend/src/components/library/LibraryPageHeader.tsx`: static library route header.
- `frontend/src/components/library/libraryBulkActions.ts`: bulk paper action result partition helper.
- `frontend/src/App.tsx`: route-level replacement of `PaperManagementPage` with `LibraryPage`.
- `frontend/src/index.css`: library workspace layout and responsive styling.
- `frontend/src/App.test.tsx`: integration coverage for route replacement, import confirmation, selection, and existing paper actions.

- [x] 1. Create library types, filter helpers, and unit coverage
  - File: `frontend/src/components/library/libraryTypes.ts`
  - File: `frontend/src/components/library/libraryFilters.ts`
  - File: `frontend/src/components/library/libraryFilters.test.ts`
  - Define `CategoryScope`, `LibraryStatusFilter`, `ImportConfirmPayload`, and helper input/output types used by later library components.
  - Implement pure helpers for category scope filtering, tag collection, paper filtering, pending counts, parse-failure counts, and duplicate-title detection.
  - Add Vitest coverage for all helper behavior, including trimmed/case-insensitive duplicate titles and combined category/search/status/tag filters.
  - Purpose: Establish stable, testable library logic before JSX components depend on it.
  - _Leverage: `frontend/src/types.ts`, existing filtering behavior in `frontend/src/components/PaperManagementPage.tsx`, existing tag filtering behavior in `frontend/src/components/PaperList.tsx`_
  - _Requirements: Requirement 2 AC 1-4, Requirement 6 AC 1 and 3, Requirement 8 AC 1-2, Requirement 17 AC 1 and 3, Requirement 18 AC 1_
  - _Prompt: Implement the task for spec paperquay-integration, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Frontend TypeScript developer. Task: create the Phase 1 library types and pure filtering helpers, then add focused Vitest tests for category scope, tag collection, combined paper filtering, pending/parse-failed counters, and duplicate-title detection. Restrictions: do not copy PaperQuay source code, do not add backend fields, do not introduce a new state library, do not modify route components in this task, and keep all helpers deterministic and side-effect free. _Leverage: reuse `Paper` and `Category` from `frontend/src/types.ts` plus the existing filtering decisions in `PaperManagementPage.tsx` and `PaperList.tsx`. _Requirements: Requirement 2 AC 1-4, Requirement 6 AC 1 and 3, Requirement 8 AC 1-2, Requirement 17 AC 1 and 3, Requirement 18 AC 1. Success: `libraryFilters.test.ts` proves the helpers work, TypeScript compiles for the new helper exports, and no application behavior changes yet. Before coding, mark this task from `[ ]` to `[-]` in `tasks.md`; after implementation and verification, call `log_implementation` with structured artifacts for created functions, then mark the task `[x]`._

- [x] 2. Add upload title support and backend regression coverage
  - File: `frontend/src/lib/api.ts`
  - File: `backend/tests/test_upload_paper.py`
  - Extend `uploadPaper` to accept `title?: string` and append a non-empty trimmed `title` to the existing `FormData`.
  - Add or update backend test coverage proving the existing `/papers/upload` route respects the submitted title over filename-derived or parser-derived fallback titles.
  - Purpose: Make import confirmation useful without changing the backend API contract or database schema.
  - _Leverage: existing `uploadPaper` implementation in `frontend/src/lib/api.ts`, existing `/papers/upload` route in `backend/app/api/routes/papers.py`, existing fixtures in `backend/tests/test_upload_paper.py` and `backend/tests/fixtures/sample.pdf`_
  - _Requirements: Requirement 1 AC 1-2, Requirement 3 AC 1-3, Requirement 4 AC 2, Requirement 15 AC 3, Requirement 18 AC 2_
  - _Prompt: Implement the task for spec paperquay-integration, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Full-stack compatibility developer. Task: extend the frontend upload wrapper so import confirmation can pass a title, and add backend regression coverage for the already-supported `/papers/upload` `title` form field. Restrictions: do not add a new backend route, do not change the response schema, do not perform a database migration, do not alter authentication behavior, and do not change upload storage semantics. _Leverage: `frontend/src/lib/api.ts`, `backend/app/api/routes/papers.py`, `backend/tests/test_upload_paper.py`, and `backend/tests/fixtures/sample.pdf`. _Requirements: Requirement 1 AC 1-2, Requirement 3 AC 1-3, Requirement 4 AC 2, Requirement 15 AC 3, Requirement 18 AC 2. Success: the frontend wrapper sends `title` only when present, backend upload tests prove title override behavior, and existing upload callers remain source-compatible. Before coding, mark this task from `[ ]` to `[-]` in `tasks.md`; after implementation and verification, call `log_implementation` with structured artifacts for modified API functions/tests, then mark the task `[x]`._

- [x] 3. Build the import confirmation dialog
  - File: `frontend/src/components/library/ImportConfirmDialog.tsx`
  - Create a modal-style dialog that accepts PDF selection/drop, derives an editable default title from the file name, accepts a source value, and shows client-side duplicate-title warnings using `findDuplicateByTitle`.
  - Submit `{ source, title, file }` through the supplied callback and preserve the dialog on upload failure.
  - Purpose: Replace the direct import form with a PaperQuay-style confirmation step while still using the current upload pipeline.
  - _Leverage: `frontend/src/components/ImportForm.tsx`, `frontend/src/components/FeedbackBanner.tsx`, `frontend/src/components/UiIcon.tsx`, `frontend/src/components/library/libraryFilters.ts`, `frontend/src/types.ts`_
  - _Requirements: Requirement 3 AC 1-4, Requirement 5 AC 1, Requirement 16 AC 3-4, Requirement 18 AC 1_
  - _Prompt: Implement the task for spec paperquay-integration, first run spec-workflow-guide to get the workflow guide then implement the task: Role: React accessibility developer. Task: create `ImportConfirmDialog` with PDF-only file selection/drop, editable title/source confirmation, duplicate-title warning, clear validation errors, submit and cancel actions, and accessible labels/focusable controls. Restrictions: do not upload before explicit confirmation, do not expose local path details beyond the selected file name, do not add quick-import behavior, do not copy PaperQuay code, and do not modify the route shell in this task. _Leverage: `ImportForm.tsx` interaction patterns, `FeedbackBanner.tsx`, `UiIcon.tsx`, and `findDuplicateByTitle` from `libraryFilters.ts`. _Requirements: Requirement 3 AC 1-4, Requirement 5 AC 1, Requirement 16 AC 3-4, Requirement 18 AC 1. Success: the dialog can validate a PDF, derive and edit a title, warn about duplicates, call `onSubmit` with `{ source, title, file }`, and keep user-entered data when upload fails. Before coding, mark this task from `[ ]` to `[-]` in `tasks.md`; after implementation and verification, call `log_implementation` with structured artifacts for the new component, then mark the task `[x]`._

- [x] 4. Build library navigation, toolbar, and dense list components
  - File: `frontend/src/components/library/LibrarySidebar.tsx`
  - File: `frontend/src/components/library/LibraryToolbar.tsx`
  - File: `frontend/src/components/library/PaperLibraryList.tsx`
  - Implement category-scope navigation, library counters, import/refresh/create-category action entry points, parse-failed batch action entry points, search input, status filter, tag chips, paper selection, and delete action forwarding.
  - Purpose: Provide the visible library workspace structure without yet wiring route-level state.
  - _Leverage: `frontend/src/components/PaperList.tsx`, `frontend/src/components/PaperActions.tsx`, `frontend/src/components/StatusBadge.tsx`, `frontend/src/components/UiIcon.tsx`, `frontend/src/components/library/libraryFilters.ts`, `frontend/src/types.ts`_
  - _Requirements: Requirement 2 AC 1-4, Requirement 6 AC 1 and 3, Requirement 7 AC 1, Requirement 8 AC 1-2, Requirement 17 AC 1-3_
  - _Prompt: Implement the task for spec paperquay-integration, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Frontend component developer. Task: create the sidebar, toolbar, and dense list components for the new library workspace using controlled props and callback-only actions. Restrictions: do not fetch data inside these components, do not mutate paper/category objects, do not implement tree category storage, do not add multi-category membership, and do not put route navigation logic in these presentational components. _Leverage: existing list/status/action UI from `PaperList.tsx`, `PaperActions.tsx`, `StatusBadge.tsx`, `UiIcon.tsx`, and helper outputs from `libraryFilters.ts`. _Requirements: Requirement 2 AC 1-4, Requirement 6 AC 1 and 3, Requirement 7 AC 1, Requirement 8 AC 1-2, Requirement 17 AC 1-3. Success: each component renders useful empty/loading states, all actions are exposed through props, search/status/tag/category controls are controlled, and selected-paper state is visually clear. Before coding, mark this task from `[ ]` to `[-]` in `tasks.md`; after implementation and verification, call `log_implementation` with structured artifacts for the created React components, then mark the task `[x]`._

- [x] 5. Build metadata and overview panels
  - File: `frontend/src/components/library/PaperMetadataPanel.tsx`
  - File: `frontend/src/components/library/PaperOverviewPanel.tsx`
  - Implement selected-paper metadata, status, category selection forwarding, tag editing surface, reader/PDF entry, pending metadata notices, and overview sections mapped from existing summary fields.
  - Purpose: Split the current monolithic detail experience into scannable library detail panels.
  - _Leverage: `frontend/src/components/PaperDetail.tsx`, `frontend/src/components/PaperActions.tsx`, `frontend/src/components/StatusBadge.tsx`, `frontend/src/components/SummaryCard.tsx`, `frontend/src/components/UiIcon.tsx`, `frontend/src/types.ts`_
  - _Requirements: Requirement 4 AC 1-3, Requirement 7 AC 1-3, Requirement 9 AC 1-4, Requirement 11 AC 1-4, Requirement 16 AC 3_
  - _Prompt: Implement the task for spec paperquay-integration, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Research-product UI developer. Task: create `PaperMetadataPanel` and `PaperOverviewPanel` so selected papers show metadata, processing status, category/tags, reader entry, and PaperQuay-style overview sections mapped from existing summary fields. Restrictions: do not add editable metadata fields that are not persisted, do not expose raw local paths as primary UI, do not create a new PDF renderer, do not remove retryable parse/summary states, and do not add schema fields in this task. _Leverage: current `PaperDetail.tsx` metadata/tag/category patterns, `StatusBadge.tsx`, `SummaryCard.tsx`, `UiIcon.tsx`, and `PaperDetail`/`Category` types. _Requirements: Requirement 4 AC 1-3, Requirement 7 AC 1-3, Requirement 9 AC 1-4, Requirement 11 AC 1-4, Requirement 16 AC 3. Success: selected-paper panels handle null/loading/error-friendly states, map existing summary fields into overview sections, keep category/tags routed through callbacks, and provide a recoverable reader/PDF entry. Before coding, mark this task from `[ ]` to `[-]` in `tasks.md`; after implementation and verification, call `log_implementation` with structured artifacts for the created React components, then mark the task `[x]`._

- [x] 6. Orchestrate the new LibraryPage and replace the route target
  - File: `frontend/src/components/library/LibraryPage.tsx`
  - File: `frontend/src/App.tsx`
  - Move the current paper-management state orchestration into `LibraryPage`: selected paper routing, detail loading, import submit, category creation, category update, tag update, parse/summarize/embed task actions, retry/delete parse-failed actions, paper delete, feedback banners, and library refresh.
  - Update `/` and `/paper/:paperId` route rendering to use `LibraryPage` while preserving existing props and routes used by briefing, recommendations, stats, subscriptions, and assistant flows.
  - Purpose: Make the new library workspace the active paper management experience without breaking existing `/papers` workflows.
  - _Leverage: `frontend/src/components/PaperManagementPage.tsx`, `frontend/src/App.tsx`, `frontend/src/lib/api.ts`, `frontend/src/components/FeedbackBanner.tsx`, all created `components/library/*` files_
  - _Requirements: Requirement 1 AC 1-3, Requirement 2 AC 1-4, Requirement 3 AC 1, Requirement 6 AC 3, Requirement 9 AC 1-3, Requirement 15 AC 1-4, Requirement 16 AC 1-2_
  - _Prompt: Implement the task for spec paperquay-integration, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Senior React integration developer. Task: create `LibraryPage` as the orchestrator for the new library components, migrate only the needed state and handlers from `PaperManagementPage`, and switch `App.tsx` routes `/` and `/paper/:paperId` to render `LibraryPage`. Restrictions: do not delete `PaperManagementPage` in this task, do not change backend routes, do not change auth flow, do not break daily briefing or recommendation navigation to `/paper/:id`, and do not add database or schema changes. _Leverage: existing route props in `App.tsx`, all API wrappers in `lib/api.ts`, state/handler behavior in `PaperManagementPage.tsx`, `FeedbackBanner.tsx`, and the new library components. _Requirements: Requirement 1 AC 1-3, Requirement 2 AC 1-4, Requirement 3 AC 1, Requirement 6 AC 3, Requirement 9 AC 1-3, Requirement 15 AC 1-4, Requirement 16 AC 1-2. Success: `/` shows the library workspace, `/paper/:paperId` loads and selects the matching paper, import creates a paper through the existing upload endpoint, parse/summarize/embed/category/tag/delete actions still refresh library state, and other app routes continue to render. Before coding, mark this task from `[ ]` to `[-]` in `tasks.md`; after implementation and verification, call `log_implementation` with structured artifacts for the orchestrator and route integration, then mark the task `[x]`._

- [x] 7. Add responsive library workspace styling
  - File: `frontend/src/index.css`
  - Add `.library-*` styles for a dense three-zone research workspace: sidebar, toolbar, paper list, detail/overview panels, import dialog, loading/empty states, status chips, tag chips, and mobile stacking.
  - Keep styles compatible with existing app theme variables and navigation layout.
  - Purpose: Deliver a usable PaperQuay-inspired library layout without turning the route into a marketing or landing page.
  - _Leverage: existing app shell styles in `frontend/src/index.css`, existing component class patterns from `PaperManagementPage`, `PaperList`, `PaperDetail`, and current design-system button/icon styles_
  - _Requirements: Requirement 2 AC 1-4, Requirement 8 AC 1-2, Requirement 9 AC 1-4, Requirement 11 AC 1-3, Requirement 17 AC 1-3_
  - _Prompt: Implement the task for spec paperquay-integration, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Product UI/CSS engineer. Task: add responsive styles for the new library workspace, import dialog, dense list, sidebar, toolbar, metadata panel, overview panel, and loading/empty states. Restrictions: do not use a one-note purple or beige palette, do not add decorative gradient blobs, do not place cards inside cards, do not use viewport-width font scaling, do not create a landing page, and ensure button text cannot overflow on mobile. _Leverage: existing `index.css` theme variables and UI patterns already used by the app shell, paper list, detail, badges, and buttons. _Requirements: Requirement 2 AC 1-4, Requirement 8 AC 1-2, Requirement 9 AC 1-4, Requirement 11 AC 1-3, Requirement 17 AC 1-3. Success: the library UI is dense, scannable, keyboard-visible, responsive at mobile and desktop widths, and visually consistent with the existing app without hiding controls. Before coding, mark this task from `[ ]` to `[-]` in `tasks.md`; after implementation and verification, call `log_implementation` with structured artifacts for CSS/layout work, then mark the task `[x]`._

- [x] 8. Add route-level integration tests and final targeted verification
  - File: `frontend/src/App.test.tsx`
  - Add frontend integration tests for the new paper management route, `/paper/:paperId` selection, import confirmation before upload, duplicate-title warning, parse/summarize/embed action preservation, and navigation from existing paper links into the new detail experience.
  - Run the focused frontend and backend verification commands and record any blocker honestly in the implementation log.
  - Purpose: Prove the replacement preserved current workflows while adding the required library shell and import confirmation behavior.
  - _Leverage: existing fetch mocks and route tests in `frontend/src/App.test.tsx`, `frontend/src/components/library/libraryFilters.test.ts`, `backend/tests/test_upload_paper.py`, existing verification commands_
  - _Requirements: Requirement 1 AC 2, Requirement 3 AC 1-4, Requirement 9 AC 1-4, Requirement 15 AC 1-4, Requirement 18 AC 1-4_
  - _Prompt: Implement the task for spec paperquay-integration, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Frontend integration test engineer. Task: update `App.test.tsx` so the route-level user flows cover the new library workspace, selected-paper detail route, import confirmation, duplicate warning, preserved paper actions, and existing navigation links into `/paper/:id`; then run focused frontend and backend verification. Restrictions: do not weaken existing assertions, do not skip failing tests without documenting an unrelated blocker, do not alter production code except for small fixes required by these tests, and do not claim completion without command output evidence. _Leverage: current `App.test.tsx` mocking setup, `libraryFilters.test.ts`, `backend/tests/test_upload_paper.py`, and the existing app route mocks. _Requirements: Requirement 1 AC 2, Requirement 3 AC 1-4, Requirement 9 AC 1-4, Requirement 15 AC 1-4, Requirement 18 AC 1-4. Success: `frontend` targeted Vitest coverage passes for `src/App.test.tsx` and `src/components/library/libraryFilters.test.ts`, backend upload regression passes for `backend/tests/test_upload_paper.py`, and any broader build/test blocker is reported with exact file and error. Before coding, mark this task from `[ ]` to `[-]` in `tasks.md`; after implementation and verification, call `log_implementation` with structured artifacts for integration tests and verification results, then mark the task `[x]`._
